<p><%= t("devise.passwords.reset_email.greeting", email: @resource.email) %></p>

<p><%= t("devise.passwords.reset_email.requested") %></p>

<p><%= link_to t("devise.passwords.reset_email.change_password"), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t("devise.passwords.reset_email.ignore") %></p>
<p><%= t("devise.passwords.reset_email.no_change") %></p>
